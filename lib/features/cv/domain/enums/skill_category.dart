enum SkillCategory {
  general('General'),
  flutter('Flutter'),
  android('Android'),
  languages('Languages'),
  additional('Additional');

  final String label;
  const SkillCategory(this.label);
}
